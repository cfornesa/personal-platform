openapi: 3.1.0
info:
  # Do not change the title, if the title changes, the import paths will be broken
  title: Api
  version: 0.1.0
  description: Microblogging API
servers:
  - url: /api
    description: Base API path
tags:
  - name: health
    description: Health operations
  - name: posts
    description: Post operations
  - name: comments
    description: Comment operations
  - name: users
    description: User operations
  - name: site
    description: Site-wide settings
  - name: feeds
    description: Inbound RSS / Atom feed sources and moderation queue
  - name: nav-links
    description: Owner-managed external navigation links rendered in the navbar
  - name: nav-items
    description: Unified nav-items operations (drag-and-drop reorder across external/page/system kinds)
  - name: pages
    description: Standalone CMS pages addressed at /p/:slug, orthogonal to posts
  - name: feeds-catalog
    description: Public catalog of subscribable site feeds (Atom / JSON Feed / MF2 export)
  - name: ai
    description: Opt-in AI writing assistant configuration and processing
  - name: art-pieces
    description: Owner-authored reusable interactive art pieces rendered in app-owned embeds
  - name: syndication
    description: POSSE outbound syndication — platform connections, OAuth app credentials, and syndication history
paths:
  /healthz:
    get:
      operationId: healthCheck
      tags: [health]
      summary: Health check
      responses:
        "200":
          description: Healthy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"

  /posts:
    get:
      operationId: listPosts
      tags: [posts]
      summary: List all posts (paginated)
      parameters:
        - in: query
          name: page
          schema:
            type: integer
            default: 1
        - in: query
          name: limit
          schema:
            type: integer
            default: 20
        - in: query
          name: category
          schema:
            type: string
          description: Filter by category slug, or the special token "uncategorized" for posts with no assigned category. Omit or pass "all" for no filter.
        - in: query
          name: source
          schema:
            type: string
          description: Filter by source. "original" for posts with no feed source (native + deleted-source posts), or a numeric feed source ID. Omit or pass "all" for no filter.
        - in: query
          name: view
          schema:
            type: string
            enum: [owner]
          description: |
            When set to "owner" (owner auth required), returns all owner-authored
            posts regardless of status (draft, scheduled, published) plus all
            RSS-imported published posts. Used by the Admin Posts calendar.
        - in: query
          name: from
          schema:
            type: string
          description: ISO date (YYYY-MM-DD). When combined with view=owner, filters posts whose createdAt/scheduledAt falls on or after this date.
        - in: query
          name: to
          schema:
            type: string
          description: ISO date (YYYY-MM-DD). When combined with view=owner, filters posts whose createdAt/scheduledAt falls on or before this date.
      responses:
        "200":
          description: List of posts
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PostsPage"
    post:
      operationId: createPost
      tags: [posts]
      summary: Create a new post
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreatePostBody"
      responses:
        "201":
          description: Created post
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Post"
        "401":
          description: Unauthorized

  /posts/drafts:
    get:
      operationId: getDraftPosts
      tags: [posts]
      summary: List owner's draft posts (owner only)
      description: Returns all posts with status='draft', sorted by createdAt descending. Used by the Admin Posts drafts section.
      responses:
        "200":
          description: List of draft posts
          content:
            application/json:
              schema:
                type: object
                properties:
                  posts:
                    type: array
                    items:
                      $ref: "#/components/schemas/Post"
                  total:
                    type: integer
                required:
                  - posts
                  - total
        "401":
          description: Unauthorized

  /posts/{id}:
    get:
      operationId: getPost
      tags: [posts]
      summary: Get a single post with its comments
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Post with comments
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PostWithComments"
        "404":
          description: Not found
    patch:
      operationId: updatePost
      tags: [posts]
      summary: Update a post (owner only)
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdatePostBody"
      responses:
        "200":
          description: Updated post
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Post"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden
        "413":
          description: Uploaded image is too large
        "404":
          description: Not found
    delete:
      operationId: deletePost
      tags: [posts]
      summary: Delete a post (owner only)
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted
        "401":
          description: Unauthorized
        "403":
          description: Forbidden
        "404":
          description: Not found

  /posts/search:
    get:
      operationId: searchPosts
      tags: [posts]
      summary: Relevance-ranked, filterable post search (published only)
      description: |
        Free-text search over post bodies (the `content_text` shadow column,
        which is the rendered text with HTML tags stripped) plus structured
        filters. Results are ranked by `MATCH ... AGAINST` score when `q` is
        present and by recency otherwise. Always restricted to
        `status = 'published'` regardless of caller — pending feed-imports
        only surface in the moderation queue.
      parameters:
        - in: query
          name: q
          schema:
            type: string
          description: Free-text query. When omitted, results fall back to newest-first.
        - in: query
          name: from
          schema:
            type: string
          description: Inclusive lower bound on `createdAt` (ISO-8601 date or datetime).
        - in: query
          name: to
          schema:
            type: string
          description: Inclusive upper bound on `createdAt` (ISO-8601 date or datetime).
        - in: query
          name: sources
          schema:
            type: string
          description: |
            Comma-separated list of `feed_sources.id` values, plus the literal
            `native` for "this site's own posts." Empty / omitted means all
            sources.
        - in: query
          name: categories
          schema:
            type: string
          description: |
            Comma-separated list of `categories.slug` values. A post matches
            when it belongs to ANY of the listed categories (OR semantics,
            mirroring `sources`). Empty / omitted means no category filter.
        - in: query
          name: author
          schema:
            type: string
          description: Case-insensitive substring match against `authorName`.
        - in: query
          name: format
          schema:
            type: string
          description: |
            Comma-separated content-format filter. Allowed: `html`, `plain`.
            Both (or neither) means no format restriction.
        - in: query
          name: page
          schema:
            type: integer
            default: 1
        - in: query
          name: limit
          schema:
            type: integer
            default: 20
      responses:
        "200":
          description: Search results
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SearchPostsPage"

  /posts/user/{userId}:
    get:
      operationId: getPostsByUser
      tags: [posts]
      summary: Get all posts by a specific user
      parameters:
        - in: path
          name: userId
          required: true
          schema:
            type: string
        - in: query
          name: page
          schema:
            type: integer
            default: 1
        - in: query
          name: limit
          schema:
            type: integer
            default: 20
      responses:
        "200":
          description: User's posts
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PostsPage"

  /posts/{postId}/comments:
    post:
      operationId: createComment
      tags: [comments]
      summary: Add a comment to a post
      parameters:
        - in: path
          name: postId
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateCommentBody"
      responses:
        "201":
          description: Created comment
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Comment"
        "401":
          description: Unauthorized

  /comments/{id}:
    patch:
      operationId: updateComment
      tags: [comments]
      summary: Update a comment
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateCommentBody"
      responses:
        "200":
          description: Updated comment
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Comment"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden
        "404":
          description: Not found
    delete:
      operationId: deleteComment
      tags: [comments]
      summary: Delete a comment (owner only)
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted
        "401":
          description: Unauthorized
        "403":
          description: Forbidden
        "404":
          description: Not found

  /users/{id}:
    get:
      operationId: getUser
      tags: [users]
      summary: Get a user profile by ID or username
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        "200":
          description: User profile
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserProfile"
        "404":
          description: Not found

  /users/me:
    get:
      operationId: getMe
      tags: [users]
      summary: Get current authenticated user info
      responses:
        "200":
          description: Current user
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserProfile"
        "401":
          description: Unauthorized
    patch:
      operationId: updateMe
      tags: [users]
      summary: Update current user profile
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateUserProfileBody"
      responses:
        "200":
          description: Updated user profile
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserProfile"
        "401":
          description: Unauthorized
        "400":
          description: Invalid request (e.g. username taken)

  /users/me/ai-settings:
    get:
      operationId: getMyAiSettings
      tags: [ai]
      summary: Get owner AI writing assistant settings
      responses:
        "200":
          description: Owner-safe AI settings for all supported vendors
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MyAiSettings"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden
    patch:
      operationId: updateMyAiSettings
      tags: [ai]
      summary: Update owner AI writing assistant settings
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateMyAiSettingsBody"
      responses:
        "200":
          description: Updated safe AI settings
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MyAiSettings"
        "400":
          description: Invalid request
        "401":
          description: Unauthorized
        "403":
          description: Forbidden

  /ai/process:
    post:
      operationId: processAiText
      tags: [ai]
      summary: Process editor content with the owner-selected AI vendor
      description: |
        Uses the owner's saved AI settings for the vendor selected in the
        editor. The request body contains editor content plus the chosen
        vendor; the model and API key come from the owner's Admin AI settings.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProcessAiTextBody"
      responses:
        "200":
          description: Expanded text result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProcessAiTextResponse"
        "400":
          description: Invalid request or provider configuration
        "401":
          description: Unauthorized
        "403":
          description: Forbidden
        "409":
          description: Selected AI vendor is not enabled for this user
        "429":
          description: Upstream provider rate limited the request

  /ai/describe-image:
    post:
      operationId: describeImage
      tags: [ai]
      summary: Generate alt text for an image URL using the owner-selected AI vendor
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                imageUrl:
                  type: string
                vendor:
                  type: string
                  enum:
                    - openrouter
                    - opencode-zen
                    - opencode-go
                    - google
                    - mistral
                    - mistral-vibe
                existingAltText:
                  type: string
                  description: Optional existing alt text to use as context for refinement
              required:
                - imageUrl
                - vendor
      responses:
        "200":
          description: Generated alt text
          content:
            application/json:
              schema:
                type: object
                properties:
                  altText:
                    type: string
                required:
                  - altText
        "400":
          description: Invalid request
        "401":
          description: Unauthorized
        "403":
          description: Forbidden
        "409":
          description: Selected AI vendor is not enabled for this user
        "422":
          description: AI model does not support image analysis
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  code:
                    type: string
                    enum:
                      - vision_not_supported
                required:
                  - error

  /art-pieces:
    get:
      operationId: listArtPieces
      tags: [art-pieces]
      summary: List the owner's reusable art pieces
      responses:
        "200":
          description: Art pieces
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListArtPiecesResponse"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden
    post:
      operationId: createArtPiece
      tags: [art-pieces]
      summary: Save a new reusable art piece
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateArtPieceBody"
      responses:
        "201":
          description: Created art piece
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateArtPieceResponse"
        "400":
          description: Invalid request
        "401":
          description: Unauthorized
        "403":
          description: Forbidden

  /art-pieces/generate:
    post:
      operationId: generateArtPiece
      tags: [art-pieces]
      summary: Generate an unsaved art piece draft with the selected AI vendor
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GenerateArtPieceBody"
      responses:
        "200":
          description: Generated art piece draft
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeneratedArtPieceDraft"
        "400":
          description: Invalid request
        "401":
          description: Unauthorized
        "403":
          description: Forbidden
        "409":
          description: Selected AI vendor is not enabled for this user

  /art-pieces/{id}:
    get:
      operationId: getArtPiece
      tags: [art-pieces]
      summary: Get one owner art piece and its versions
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Art piece
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ArtPieceDetail"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden
        "404":
          description: Not found
    patch:
      operationId: updateArtPiece
      tags: [art-pieces]
      summary: Update owner-controlled art piece metadata
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateArtPieceBody"
      responses:
        "200":
          description: Updated art piece
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ArtPiece"
        "400":
          description: Invalid request
        "401":
          description: Unauthorized
        "403":
          description: Forbidden
        "404":
          description: Not found
    delete:
      operationId: deleteArtPiece
      tags: [art-pieces]
      summary: Delete an art piece and all its versions (owner only)
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted
        "401":
          description: Unauthorized
        "403":
          description: Forbidden
        "404":
          description: Not found

  /art-pieces/{id}/versions:
    post:
      operationId: createArtPieceVersion
      tags: [art-pieces]
      summary: Save a new version for an existing art piece
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateArtPieceVersionBody"
      responses:
        "201":
          description: Created art piece version
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateArtPieceVersionResponse"
        "400":
          description: Invalid request
        "401":
          description: Unauthorized
        "403":
          description: Forbidden
        "404":
          description: Not found

  /art-pieces/{id}/embed:
    get:
      operationId: getEmbeddedArtPiece
      tags: [art-pieces]
      summary: Get the public runtime payload for an embedded art piece
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
        - in: query
          name: version
          required: false
          schema:
            type: integer
      responses:
        "200":
          description: Embedded art piece payload
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmbeddedArtPiece"
        "404":
          description: Not found

  /feed/stats:
    get:
      operationId: getFeedStats
      tags: [posts]
      summary: Get feed statistics (total posts, active users today)
      responses:
        "200":
          description: Feed statistics
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FeedStats"

  /media:
    get:
      operationId: listMedia
      tags: [posts]
      summary: List all uploaded media assets (owner only)
      responses:
        "200":
          description: List of media assets ordered by upload date descending
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/MediaAsset"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden
    post:
      operationId: uploadMedia
      tags: [posts]
      summary: Upload a media asset for a post (owner only)
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                file:
                  type: string
                  format: binary
              required:
                - file
      responses:
        "201":
          description: Uploaded media metadata
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UploadedMedia"
        "400":
          description: Invalid upload
        "401":
          description: Unauthorized
        "403":
          description: Forbidden

  /media/import:
    post:
      operationId: importMedia
      tags: [posts]
      summary: Import a remote image URL into the media library (owner only)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ImportMediaBody"
      responses:
        "201":
          description: Imported media metadata
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UploadedMedia"
        "400":
          description: Invalid image URL
        "401":
          description: Unauthorized
        "403":
          description: Forbidden
        "413":
          description: Imported image is too large

  /site-settings:
    get:
      operationId: getSiteSettings
      tags: [site]
      summary: Get site-wide settings (title, copy, palette)
      responses:
        "200":
          description: Site settings
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SiteSettings"
    patch:
      operationId: updateSiteSettings
      tags: [site]
      summary: Update site-wide settings (owner only)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateSiteSettingsBody"
      responses:
        "200":
          description: Updated site settings
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SiteSettings"
        "400":
          description: Invalid request
        "401":
          description: Unauthorized
        "403":
          description: Forbidden

  /posts/pending:
    get:
      operationId: listPendingPosts
      tags: [feeds]
      summary: List posts awaiting moderation (owner only)
      parameters:
        - in: query
          name: page
          schema:
            type: integer
            default: 1
        - in: query
          name: limit
          schema:
            type: integer
            default: 50
      responses:
        "200":
          description: Pending queue
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PendingPostsPage"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden

  /posts/{id}/approve:
    post:
      operationId: approvePost
      tags: [feeds]
      summary: Approve a pending post (owner only)
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Approval result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApprovePostResponse"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden
        "404":
          description: Not found
        "409":
          description: Post is not pending

  /posts/{id}/reject:
    post:
      operationId: rejectPost
      tags: [feeds]
      summary: Reject (delete) a pending post (owner only)
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Rejected
        "401":
          description: Unauthorized
        "403":
          description: Forbidden
        "404":
          description: Not found
        "409":
          description: Post is not pending

  /feed-sources/public:
    get:
      operationId: listPublicFeedSources
      tags: [feeds]
      summary: Public list of feed sources that have at least one published post
      description: >
        Returns id+name only — no feed URL or fetch state — for any visitor
        to power the source filter on the search results page.
      responses:
        "200":
          description: Public sources
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PublicFeedSourcesList"

  /feed-sources:
    get:
      operationId: listFeedSources
      tags: [feeds]
      summary: List configured RSS / Atom sources (owner only)
      responses:
        "200":
          description: Sources
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FeedSourcesList"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden
    post:
      operationId: createFeedSource
      tags: [feeds]
      summary: Create a new feed source (owner only)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateFeedSourceBody"
      responses:
        "201":
          description: Created source
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FeedSource"
        "400":
          description: Invalid request
        "401":
          description: Unauthorized
        "403":
          description: Forbidden

  /feed-sources/{id}:
    patch:
      operationId: updateFeedSource
      tags: [feeds]
      summary: Update a feed source (owner only)
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateFeedSourceBody"
      responses:
        "200":
          description: Updated source
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FeedSource"
        "400":
          description: Invalid request
        "401":
          description: Unauthorized
        "403":
          description: Forbidden
        "404":
          description: Not found
    delete:
      operationId: deleteFeedSource
      tags: [feeds]
      summary: Delete a feed source (owner only)
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted
        "401":
          description: Unauthorized
        "403":
          description: Forbidden
        "404":
          description: Not found

  /feed-sources/{id}/refresh:
    post:
      operationId: refreshFeedSource
      tags: [feeds]
      summary: Queue this feed source for an immediate background refresh (owner only)
      description: |
        Schedules a refresh on the background fetch worker and returns immediately.
        Outcome of the actual fetch (imported / skipped counts, upstream errors)
        lands in the source row's `last_status` / `last_error` columns and is
        visible on the next list reload. Returns `status: "ok"` when the work
        was queued; the response's `fetched` / `imported` / `skipped` counters
        are zero because the fetch has not run yet.
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Refresh result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FeedRefreshResult"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden
        "404":
          description: Not found

  /feed-sources/{id}/approve-all:
    post:
      operationId: approveAllFromFeedSource
      tags: [feeds]
      summary: Bulk-approve every pending post from this source (owner only)
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Number of pending posts approved
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApproveAllFromFeedSourceResponse"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden
        "404":
          description: Not found

  /feed-sources/refresh:
    post:
      operationId: refreshAllFeedSources
      tags: [feeds]
      summary: Refresh every enabled, due source (owner cookie or X-Cron-Secret)
      parameters:
        - in: query
          name: force
          schema:
            type: string
            enum: ["1", "true"]
          description: When set, ignore each source's cadence and pull every enabled source.
      responses:
        "200":
          description: Aggregate refresh result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FeedRefreshSummary"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden

  /categories:
    get:
      operationId: listCategories
      tags: [categories]
      summary: List every category with its post count
      description: |
        Public read. Returns all categories ordered by name, each enriched with
        a `postCount` of *published* posts attached to it.
      responses:
        "200":
          description: Categories list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CategoriesList"
    post:
      operationId: createCategory
      tags: [categories]
      summary: Create a new category (owner only)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateCategoryBody"
      responses:
        "201":
          description: Created category
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Category"
        "400":
          description: Invalid body
        "401":
          description: Unauthorized
        "403":
          description: Forbidden
        "409":
          description: Slug already exists

  /categories/{slug}:
    get:
      operationId: getCategory
      tags: [categories]
      summary: Get a single category by slug
      parameters:
        - in: path
          name: slug
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Category
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CategoryWithPostCount"
        "404":
          description: Not found

  /categories/{id}:
    patch:
      operationId: updateCategory
      tags: [categories]
      summary: Update a category by id (owner only)
      description: |
        Writes are keyed by the stable internal `id` rather than the
        mutable `slug` so that renames (which change the slug) do not
        race against in-flight management UI requests. Express routes
        both `GET /categories/{slug}` and `PATCH/DELETE
        /categories/{id}` on the same path pattern; method dispatch is
        unambiguous.
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateCategoryBody"
      responses:
        "200":
          description: Updated category
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Category"
        "400":
          description: Invalid body
        "401":
          description: Unauthorized
        "403":
          description: Forbidden
        "404":
          description: Not found
        "409":
          description: Slug already exists
    delete:
      operationId: deleteCategory
      tags: [categories]
      summary: Delete a category by id (owner only)
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted
        "401":
          description: Unauthorized
        "403":
          description: Forbidden
        "404":
          description: Not found

  /categories/{slug}/posts:
    get:
      operationId: getCategoryPosts
      tags: [categories]
      summary: List published posts attached to a category
      parameters:
        - in: path
          name: slug
          required: true
          schema:
            type: string
        - in: query
          name: page
          schema:
            type: integer
            default: 1
        - in: query
          name: limit
          schema:
            type: integer
            default: 20
      responses:
        "200":
          description: Category posts page
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PostsPage"
        "404":
          description: Not found

  /nav-links:
    get:
      operationId: listNavLinks
      tags: [nav-links]
      summary: List the owner-configured navbar links
      description: |
        Public read. Returns every visible nav link sorted ascending by
        `sortOrder`. Owners may pass `includeHidden=1` to also receive
        rows with `visible=false` (used by /admin/navigation and
        /admin/pages so the owner can manage hidden rows).
      parameters:
        - in: query
          name: includeHidden
          required: false
          schema:
            type: string
            enum: ["1"]
          description: When `1` and the caller is the owner, includes hidden rows.
      responses:
        "200":
          description: Nav-links list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NavLinksList"
    post:
      operationId: createNavLink
      tags: [nav-links]
      summary: Create a new nav link (owner only)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateNavLinkBody"
      responses:
        "201":
          description: Created nav link
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NavLink"
        "400":
          description: Invalid body
        "401":
          description: Unauthorized
        "403":
          description: Forbidden

  /nav-links/{id}:
    patch:
      operationId: updateNavLink
      tags: [nav-links]
      summary: Update a nav link (owner only)
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateNavLinkBody"
      responses:
        "200":
          description: Updated nav link
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NavLink"
        "400":
          description: Invalid body
        "401":
          description: Unauthorized
        "403":
          description: Forbidden
        "404":
          description: Not found
    delete:
      operationId: deleteNavLink
      tags: [nav-links]
      summary: Delete a nav link (owner only)
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted
        "401":
          description: Unauthorized
        "403":
          description: Forbidden
        "404":
          description: Not found

  /nav-items/reorder:
    patch:
      operationId: reorderNavItems
      tags: [nav-items]
      summary: Persist a new order for nav items (owner only)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/NavItemsReorderBody"
      responses:
        "200":
          description: Reordered nav items list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NavLinksList"
        "400":
          description: Invalid body
        "401":
          description: Unauthorized
        "403":
          description: Forbidden

  /pages:
    get:
      operationId: listPages
      tags: [pages]
      summary: List standalone pages
      parameters:
        - in: query
          name: includeDrafts
          schema:
            type: string
          description: When `1`, also returns drafts (owner only).
      responses:
        "200":
          description: Pages list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagesList"
    post:
      operationId: createPage
      tags: [pages]
      summary: Create a new page (owner only)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreatePageBody"
      responses:
        "201":
          description: Created page
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Page"
        "400":
          description: Invalid body / reserved slug / slug collision
        "401":
          description: Unauthorized
        "403":
          description: Forbidden

  /pages/{slug}:
    get:
      operationId: getPageBySlug
      tags: [pages]
      summary: Fetch a single page by slug (drafts 404 for non-owners)
      parameters:
        - in: path
          name: slug
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Page
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Page"
        "404":
          description: Not found

  /pages/{id}:
    patch:
      operationId: updatePage
      tags: [pages]
      summary: Update a page (owner only)
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdatePageBody"
      responses:
        "200":
          description: Updated page
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Page"
        "400":
          description: Invalid body / reserved slug / slug collision
        "401":
          description: Unauthorized
        "403":
          description: Forbidden
        "404":
          description: Not found
    delete:
      operationId: deletePage
      tags: [pages]
      summary: Delete a page (owner only). Cascade-deletes its nav row.
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted
        "401":
          description: Unauthorized
        "403":
          description: Forbidden
        "404":
          description: Not found

  /feeds:
    get:
      operationId: listSiteFeeds
      tags: [feeds-catalog]
      summary: Public catalog of subscribable site feeds
      description: |
        Hand-maintained list of the three sitewide feeds the platform
        produces (Atom, JSON Feed, MF2 export). Used by the public
        `/feeds` index page in the microblog UI.

        When called with `?category=<slug>` the response also includes
        the per-category Atom and JSON feeds; with `?page=<slug>` it
        includes the per-page Atom and JSON feeds for the matching
        published CMS page. Unknown slugs are silently ignored.
      parameters:
        - in: query
          name: category
          required: false
          schema:
            type: string
          description: Optional category slug; appends per-category feeds when set.
        - in: query
          name: page
          required: false
          schema:
            type: string
          description: Optional CMS page slug; appends per-page feeds when set.
      responses:
        "200":
          description: Site feeds list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SiteFeedsList"

  /media/{fileName}:
    patch:
      operationId: updateMediaAltText
      tags: [posts]
      summary: Update title and alt text for an uploaded media asset (owner only)
      parameters:
        - in: path
          name: fileName
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateMediaAltTextBody"
      responses:
        "200":
          description: Updated media asset
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MediaAsset"
        "400":
          description: Invalid request
        "401":
          description: Unauthorized
        "403":
          description: Forbidden
        "404":
          description: Not found
    get:
      operationId: getMedia
      tags: [posts]
      summary: Fetch an uploaded media asset
      parameters:
        - in: path
          name: fileName
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Media content
        "404":
          description: Not found
    delete:
      operationId: deleteMedia
      tags: [posts]
      summary: Permanently delete an uploaded media asset (owner only)
      parameters:
        - in: path
          name: fileName
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Deleted
        "401":
          description: Unauthorized
        "403":
          description: Forbidden
        "404":
          description: Not found

  /platform-oauth-apps:
    get:
      operationId: listPlatformOAuthApps
      tags: [syndication]
      summary: List OAuth app credential status for all supported platforms (owner only)
      description: |
        Returns one entry per OAuth-app platform (`wordpress_com`, `blogger`, `linkedin`, `facebook`) with a
        `configured` flag. The actual CLIENT_ID and CLIENT_SECRET are never returned.
      responses:
        "200":
          description: OAuth app list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PlatformOAuthAppsList"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden

  /platform-oauth-apps/{platform}:
    put:
      operationId: upsertPlatformOAuthApp
      tags: [syndication]
      summary: Save or update OAuth app credentials for a platform (owner only)
      parameters:
        - in: path
          name: platform
          required: true
          schema:
            type: string
            enum: [wordpress_com, blogger, linkedin, facebook]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpsertPlatformOAuthAppBody"
      responses:
        "200":
          description: Updated OAuth app status
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PlatformOAuthApp"
        "400":
          description: Invalid body or unsupported platform
        "401":
          description: Unauthorized
        "403":
          description: Forbidden

  /platform-connections:
    get:
      operationId: listPlatformConnections
      tags: [syndication]
      summary: List the owner's platform connections (owner only)
      responses:
        "200":
          description: Platform connections list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PlatformConnectionsList"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden
    post:
      operationId: createPlatformConnection
      tags: [syndication]
      summary: Create or update a credential-based platform connection (owner only)
      description: |
        Used for credential-based platforms (`wordpress_self`, `medium`, `substack`). OAuth-based
        platforms (`wordpress_com`, `blogger`, `linkedin`, `facebook`) create their
        connection via the OAuth callback route at
        `/api/platform-oauth/{platform}/start`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreatePlatformConnectionBody"
      responses:
        "200":
          description: Updated existing connection
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PlatformConnection"
        "201":
          description: Created new connection
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PlatformConnection"
        "400":
          description: Invalid body or unsupported platform
        "401":
          description: Unauthorized
        "403":
          description: Forbidden

  /platform-connections/{id}:
    patch:
      operationId: updatePlatformConnection
      tags: [syndication]
      summary: Enable or disable a platform connection (owner only)
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PatchPlatformConnectionBody"
      responses:
        "200":
          description: Updated connection
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PlatformConnection"
        "400":
          description: Invalid body
        "401":
          description: Unauthorized
        "403":
          description: Forbidden
        "404":
          description: Not found
    delete:
      operationId: deletePlatformConnection
      tags: [syndication]
      summary: Disconnect and delete a platform connection (owner only)
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted
        "401":
          description: Unauthorized
        "403":
          description: Forbidden
        "404":
          description: Not found

  /platform-connections/{id}/syndications:
    get:
      operationId: listPlatformSyndications
      tags: [syndication]
      summary: List syndication history for a platform connection (owner only)
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Syndication history
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PostSyndicationsList"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden
        "404":
          description: Not found

components:
  schemas:
    HealthStatus:
      type: object
      properties:
        status:
          type: string
      required:
        - status

    Category:
      type: object
      description: Owner-managed taxonomy entry. Slug is the canonical identifier in URLs.
      properties:
        id:
          type: integer
        slug:
          type: string
        name:
          type: string
        description:
          type: string
          nullable: true
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
      required:
        - id
        - slug
        - name
        - createdAt
        - updatedAt

    CategoryWithPostCount:
      type: object
      properties:
        id:
          type: integer
        slug:
          type: string
        name:
          type: string
        description:
          type: string
          nullable: true
        postCount:
          type: integer
          description: Number of published posts attached to this category.
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
      required:
        - id
        - slug
        - name
        - postCount
        - createdAt
        - updatedAt

    CategoriesList:
      type: object
      properties:
        categories:
          type: array
          items:
            $ref: "#/components/schemas/CategoryWithPostCount"
      required:
        - categories

    CreateCategoryBody:
      type: object
      properties:
        name:
          type: string
          maxLength: 255
        slug:
          type: string
          maxLength: 191
          description: Optional. When omitted the server derives one from `name`.
        description:
          type: string
          nullable: true
      required:
        - name

    UpdateCategoryBody:
      type: object
      properties:
        name:
          type: string
          maxLength: 255
        slug:
          type: string
          maxLength: 191
        description:
          type: string
          nullable: true

    Post:
      type: object
      properties:
        id:
          type: integer
        authorId:
          type: string
        authorName:
          type: string
        authorImageUrl:
          type: string
          nullable: true
        title:
          type: string
          nullable: true
          maxLength: 500
          description: Optional post title. Null for title-less microblog posts.
        content:
          type: string
        contentFormat:
          type: string
          enum: [plain, html]
        commentCount:
          type: integer
        sourceFeedId:
          type: integer
          nullable: true
          description: ID of the feed_sources row that imported this post (PESOS); null for owner-authored posts.
        sourceFeedName:
          type: string
          nullable: true
          description: Display name of the originating feed source (joined from feed_sources). Null for owner-authored posts.
        sourceCanonicalUrl:
          type: string
          nullable: true
          description: Permalink of the post on its origin site (PESOS attribution).
        categories:
          type: array
          description: Categories this post belongs to (owner-managed taxonomy).
          items:
            $ref: "#/components/schemas/Category"
        status:
          type: string
          enum: [published, pending, draft, scheduled]
          description: |
            Publication status of the post. 'published' = live on the public timeline;
            'pending' = RSS moderation queue; 'draft' = saved but not yet published;
            'scheduled' = auto-publish at scheduledAt.
        scheduledAt:
          type: string
          format: date-time
          nullable: true
          description: ISO 8601 datetime when a 'scheduled' post will be auto-published. Null for non-scheduled posts.
        pendingPlatformIds:
          type: array
          nullable: true
          description: Platform connection IDs to syndicate when this draft/scheduled post is published. Null after publishing.
          items:
            type: integer
            minimum: 1
        syndications:
          type: array
          description: Platforms this post was successfully cross-posted to (POSSE). Omitted when none.
          items:
            $ref: "#/components/schemas/PostSyndicationBadge"
        featuredImageUrl:
          type: string
          nullable: true
          description: Optional featured image URL used for og:image and social media posts.
        socialPostDrafts:
          type: object
          nullable: true
          description: Per-platform social post captions (bluesky, linkedin, facebook, instagram). Null when not set.
          properties:
            bluesky:
              type: string
            linkedin:
              type: string
            facebook:
              type: string
            instagram:
              type: string
        createdAt:
          type: string
          format: date-time
      required:
        - id
        - authorId
        - authorName
        - content
        - contentFormat
        - commentCount
        - categories
        - createdAt

    PostsPage:
      type: object
      properties:
        posts:
          type: array
          items:
            $ref: "#/components/schemas/Post"
        total:
          type: integer
        page:
          type: integer
        limit:
          type: integer
      required:
        - posts
        - total
        - page
        - limit

    SearchPost:
      type: object
      description: |
        A post in a search result list. Extends the base `Post` shape with a
        server-rendered HTML `snippet` (already escaped + wrapped in `<mark>`
        tags around matched terms) and an optional relevance `score`. The
        score is omitted when the request had no `q` parameter.
      properties:
        id:
          type: integer
        authorId:
          type: string
        authorName:
          type: string
        authorImageUrl:
          type: string
          nullable: true
        content:
          type: string
        contentFormat:
          type: string
          enum: [plain, html]
        commentCount:
          type: integer
        sourceFeedId:
          type: integer
          nullable: true
        sourceFeedName:
          type: string
          nullable: true
        sourceCanonicalUrl:
          type: string
          nullable: true
        categories:
          type: array
          items:
            $ref: "#/components/schemas/Category"
        createdAt:
          type: string
          format: date-time
        snippet:
          type: string
          description: |
            HTML-safe excerpt of `content_text` centered on the first matched
            term, with `<mark>` tags around matched tokens. Render with
            `dangerouslySetInnerHTML` — sanitization happens server-side.
        score:
          type: number
          nullable: true
          description: MySQL FULLTEXT relevance score; omitted when `q` was empty.
      required:
        - id
        - authorId
        - authorName
        - content
        - contentFormat
        - commentCount
        - categories
        - createdAt
        - snippet

    SearchPostsPage:
      type: object
      properties:
        posts:
          type: array
          items:
            $ref: "#/components/schemas/SearchPost"
        total:
          type: integer
        page:
          type: integer
        limit:
          type: integer
        query:
          type: string
          description: Echo of the `q` parameter the server actually used (post-trim).
      required:
        - posts
        - total
        - page
        - limit
        - query

    Comment:
      type: object
      properties:
        id:
          type: integer
        postId:
          type: integer
        authorId:
          type: string
        authorName:
          type: string
        authorImageUrl:
          type: string
          nullable: true
        content:
          type: string
        createdAt:
          type: string
          format: date-time
      required:
        - id
        - postId
        - authorId
        - authorName
        - content
        - createdAt

    PostWithComments:
      type: object
      properties:
        post:
          $ref: "#/components/schemas/Post"
        comments:
          type: array
          items:
            $ref: "#/components/schemas/Comment"
      required:
        - post
        - comments

    CreatePostBody:
      type: object
      properties:
        title:
          type: string
          maxLength: 500
          description: Optional post title. Omit or send empty string for title-less microblog posts.
        content:
          type: string
          maxLength: 40000
        contentFormat:
          type: string
          enum: [plain, html]
        categoryIds:
          type: array
          description: |
            Optional list of `categories.id` values to attach to the new post.
            Every id must be a positive integer; any unknown or malformed id
            causes the request to fail with 400 and no post is created.
            Omitting the field (or sending an empty array) leaves the post
            uncategorized.
          items:
            type: integer
            minimum: 1
        platformIds:
          type: array
          description: |
            Optional list of `platform_connections.id` values to syndicate to
            after the post is created (POSSE). Only connections owned by the
            authenticated user and with `enabled=true` are dispatched.
            Omitting the field (or sending an empty array) skips syndication.
          items:
            type: integer
            minimum: 1
        substackSendNewsletter:
          type: boolean
          description: |
            Optional Substack-only delivery mode. When true and a selected
            Substack connection is included in `platformIds`, the post is both
            published on Substack and sent as a newsletter. Defaults to false.
        status:
          type: string
          enum: [published, draft, scheduled]
          default: published
          description: |
            'published' (default) = immediately visible on the public timeline.
            'draft' = saved but not published; platformIds stored as pendingPlatformIds.
            'scheduled' = auto-publish at scheduledAt; platformIds stored as pendingPlatformIds.
        scheduledAt:
          type: string
          format: date-time
          description: Required when status='scheduled'. Must be at least 1 hour in the future (ISO 8601).
        featuredImageUrl:
          type: string
          nullable: true
          description: Optional featured image URL for og:image and social media posts.
        socialPostDrafts:
          type: object
          nullable: true
          description: Per-platform social post captions to use when auto-posting to Bluesky, LinkedIn, Facebook, or Instagram.
          properties:
            bluesky:
              type: string
            linkedin:
              type: string
            facebook:
              type: string
            instagram:
              type: string
      required:
        - content
        - contentFormat

    UpdatePostBody:
      type: object
      properties:
        title:
          type: string
          maxLength: 500
          description: Optional post title. Omit to leave unchanged; send empty string to clear.
        content:
          type: string
          maxLength: 40000
        contentFormat:
          type: string
          enum: [plain, html]
        categoryIds:
          type: array
          description: |
            Replaces the post's category set. Sending an empty array clears all
            categories; omitting the field leaves the existing set untouched.
            Every id must be a positive integer; any unknown or malformed id
            causes the request to fail with 400 and the post stays unchanged.
          items:
            type: integer
            minimum: 1
        platformIds:
          type: array
          description: |
            For draft/scheduled posts: replaces the pending platform connection IDs
            to syndicate when the post is published. For already-published posts:
            triggers immediate syndication (same as create). Omitting the field
            leaves the existing pendingPlatformIds untouched.
          items:
            type: integer
            minimum: 1
        status:
          type: string
          enum: [published, draft, scheduled]
          description: |
            Transition the post's publication status. Allowed transitions:
            draft→published (fires syndication), draft→scheduled,
            scheduled→published (fires syndication), scheduled→draft.
            published→draft is logged but does not recall external syndications.
        scheduledAt:
          type: string
          format: date-time
          nullable: true
          description: Required when transitioning to status='scheduled'. Send null to clear when moving to draft or published.
        featuredImageUrl:
          type: string
          nullable: true
          description: Optional featured image URL. Send null to clear.
        socialPostDrafts:
          type: object
          nullable: true
          description: Per-platform social post captions. Send null to clear.
          properties:
            bluesky:
              type: string
            linkedin:
              type: string
            facebook:
              type: string
            instagram:
              type: string
      required:
        - content
        - contentFormat

    CreateCommentBody:
      type: object
      properties:
        content:
          type: string
          maxLength: 500
      required:
        - content

    UpdateCommentBody:
      type: object
      properties:
        content:
          type: string
          maxLength: 500
      required:
        - content

    UserProfile:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        username:
          type: string
          nullable: true
        imageUrl:
          type: string
          nullable: true
        bio:
          type: string
          nullable: true
        website:
          type: string
          nullable: true
        socialLinks:
          type: object
          additionalProperties:
            type: string
          nullable: true
        postCount:
          type: integer
        theme:
          type: string
          enum:
            - bauhaus
            - traditional
            - minimalist
            - academic
            - airy
            - nature
            - comfort
            - audacious
            - artistic
          nullable: true
        palette:
          type: string
          enum:
            - bauhaus
            - monochrome
            - newsprint
            - ocean
            - forest
            - sunset
            - sepia
            - high-contrast
            - pastel
          nullable: true
        colorBackground:
          type: string
          nullable: true
        colorForeground:
          type: string
          nullable: true
        colorBackgroundDark:
          type: string
          nullable: true
        colorForegroundDark:
          type: string
          nullable: true
        colorPrimary:
          type: string
          nullable: true
        colorPrimaryForeground:
          type: string
          nullable: true
        colorSecondary:
          type: string
          nullable: true
        colorSecondaryForeground:
          type: string
          nullable: true
        colorAccent:
          type: string
          nullable: true
        colorAccentForeground:
          type: string
          nullable: true
        colorMuted:
          type: string
          nullable: true
        colorMutedForeground:
          type: string
          nullable: true
        colorDestructive:
          type: string
          nullable: true
        colorDestructiveForeground:
          type: string
          nullable: true
        sourceType:
          type: string
          enum:
            - feed
          nullable: true
          description: Present only when the profile represents an external blog feed source rather than a human user.
        siteUrl:
          type: string
          nullable: true
          description: The blog's canonical site URL. Present when sourceType is "feed".
      required:
        - id
        - name
        - postCount

    UpdateUserProfileBody:
      type: object
      description: |
        Partial update to the current user's profile. Theme columns
        (`theme`, `palette`, and the 14 `color*` fields) accept an explicit
        `null` to clear the user's customization for that column, which
        makes their profile fall back to the site-wide theme value.
      properties:
        name:
          type: string
          maxLength: 255
          pattern: ".*\\S.*"
        username:
          type: string
          pattern: "^[a-zA-Z0-9_]{3,30}$"
        bio:
          type: string
          maxLength: 500
        website:
          type: string
          format: uri
        socialLinks:
          type: object
          additionalProperties:
            type: string
        theme:
          type: string
          nullable: true
          enum:
            - bauhaus
            - traditional
            - minimalist
            - academic
            - airy
            - nature
            - comfort
            - audacious
            - artistic
        palette:
          type: string
          nullable: true
          enum:
            - bauhaus
            - monochrome
            - newsprint
            - ocean
            - forest
            - sunset
            - sepia
            - high-contrast
            - pastel
        colorBackground:
          type: string
          nullable: true
          maxLength: 32
          pattern: "^[0-9]{1,3}(\\.[0-9]+)? [0-9]{1,3}(\\.[0-9]+)?% [0-9]{1,3}(\\.[0-9]+)?%$"
        colorForeground:
          type: string
          nullable: true
          maxLength: 32
          pattern: "^[0-9]{1,3}(\\.[0-9]+)? [0-9]{1,3}(\\.[0-9]+)?% [0-9]{1,3}(\\.[0-9]+)?%$"
        colorBackgroundDark:
          type: string
          nullable: true
          maxLength: 32
          pattern: "^[0-9]{1,3}(\\.[0-9]+)? [0-9]{1,3}(\\.[0-9]+)?% [0-9]{1,3}(\\.[0-9]+)?%$"
        colorForegroundDark:
          type: string
          nullable: true
          maxLength: 32
          pattern: "^[0-9]{1,3}(\\.[0-9]+)? [0-9]{1,3}(\\.[0-9]+)?% [0-9]{1,3}(\\.[0-9]+)?%$"
        colorPrimary:
          type: string
          nullable: true
          maxLength: 32
          pattern: "^[0-9]{1,3}(\\.[0-9]+)? [0-9]{1,3}(\\.[0-9]+)?% [0-9]{1,3}(\\.[0-9]+)?%$"
        colorPrimaryForeground:
          type: string
          nullable: true
          maxLength: 32
          pattern: "^[0-9]{1,3}(\\.[0-9]+)? [0-9]{1,3}(\\.[0-9]+)?% [0-9]{1,3}(\\.[0-9]+)?%$"
        colorSecondary:
          type: string
          nullable: true
          maxLength: 32
          pattern: "^[0-9]{1,3}(\\.[0-9]+)? [0-9]{1,3}(\\.[0-9]+)?% [0-9]{1,3}(\\.[0-9]+)?%$"
        colorSecondaryForeground:
          type: string
          nullable: true
          maxLength: 32
          pattern: "^[0-9]{1,3}(\\.[0-9]+)? [0-9]{1,3}(\\.[0-9]+)?% [0-9]{1,3}(\\.[0-9]+)?%$"
        colorAccent:
          type: string
          nullable: true
          maxLength: 32
          pattern: "^[0-9]{1,3}(\\.[0-9]+)? [0-9]{1,3}(\\.[0-9]+)?% [0-9]{1,3}(\\.[0-9]+)?%$"
        colorAccentForeground:
          type: string
          nullable: true
          maxLength: 32
          pattern: "^[0-9]{1,3}(\\.[0-9]+)? [0-9]{1,3}(\\.[0-9]+)?% [0-9]{1,3}(\\.[0-9]+)?%$"
        colorMuted:
          type: string
          nullable: true
          maxLength: 32
          pattern: "^[0-9]{1,3}(\\.[0-9]+)? [0-9]{1,3}(\\.[0-9]+)?% [0-9]{1,3}(\\.[0-9]+)?%$"
        colorMutedForeground:
          type: string
          nullable: true
          maxLength: 32
          pattern: "^[0-9]{1,3}(\\.[0-9]+)? [0-9]{1,3}(\\.[0-9]+)?% [0-9]{1,3}(\\.[0-9]+)?%$"
        colorDestructive:
          type: string
          nullable: true
          maxLength: 32
          pattern: "^[0-9]{1,3}(\\.[0-9]+)? [0-9]{1,3}(\\.[0-9]+)?% [0-9]{1,3}(\\.[0-9]+)?%$"
        colorDestructiveForeground:
          type: string
          nullable: true
          maxLength: 32
          pattern: "^[0-9]{1,3}(\\.[0-9]+)? [0-9]{1,3}(\\.[0-9]+)?% [0-9]{1,3}(\\.[0-9]+)?%$"

    AiVendorOption:
      type: object
      properties:
        id:
          type: string
          enum:
            - openrouter
            - opencode-zen
            - opencode-go
            - google
            - mistral
            - mistral-vibe
        label:
          type: string
      required:
        - id
        - label

    MyAiVendorSetting:
      type: object
      properties:
        vendor:
          type: string
          enum:
            - openrouter
            - opencode-zen
            - opencode-go
            - google
            - mistral
            - mistral-vibe
        vendorLabel:
          type: string
        enabled:
          type: boolean
        configured:
          type: boolean
        model:
          type: string
          nullable: true
      required:
        - vendor
        - vendorLabel
        - enabled
        - configured
    
    MyAiSettings:
      type: object
      properties:
        availableVendors:
          type: array
          items:
            $ref: "#/components/schemas/AiVendorOption"
        settings:
          type: array
          items:
            $ref: "#/components/schemas/MyAiVendorSetting"
        preferredArtPieceVendor:
          type: string
          nullable: true
          enum:
            - openrouter
            - opencode-zen
            - opencode-go
            - google
            - mistral
            - mistral-vibe
        preferredVendorTextImprove:
          type: string
          nullable: true
          enum:
            - openrouter
            - opencode-zen
            - opencode-go
            - google
            - mistral
            - mistral-vibe
        preferredVendorAltText:
          type: string
          nullable: true
          enum:
            - openrouter
            - opencode-zen
            - opencode-go
            - google
            - mistral
            - mistral-vibe
      required:
        - availableVendors
        - settings
        - preferredArtPieceVendor
        - preferredVendorTextImprove
        - preferredVendorAltText

    UpdateMyAiVendorSettingBody:
      type: object
      properties:
        vendor:
          type: string
          enum:
            - openrouter
            - opencode-zen
            - opencode-go
            - google
            - mistral
            - mistral-vibe
        enabled:
          type: boolean
        model:
          type: string
          minLength: 1
          maxLength: 191
        apiKey:
          type: string
          minLength: 1
          maxLength: 4096
      required:
        - vendor

    UpdateMyAiSettingsBody:
      type: object
      description: |
        Owner AI settings for all supported vendors. Each vendor keeps its own
        enabled flag, model slug, and encrypted API key so the editor can
        switch vendors without re-entering credentials.
      properties:
        settings:
          type: array
          items:
            $ref: "#/components/schemas/UpdateMyAiVendorSettingBody"
        preferredArtPieceVendor:
          type: string
          nullable: true
          enum:
            - openrouter
            - opencode-zen
            - opencode-go
            - google
            - mistral
            - mistral-vibe
        preferredVendorTextImprove:
          type: string
          nullable: true
          enum:
            - openrouter
            - opencode-zen
            - opencode-go
            - google
            - mistral
            - mistral-vibe
        preferredVendorAltText:
          type: string
          nullable: true
          enum:
            - openrouter
            - opencode-zen
            - opencode-go
            - google
            - mistral
            - mistral-vibe
      required:
        - settings

    ProcessAiTextBody:
      type: object
      properties:
        content:
          type: string
          maxLength: 40000
        vendor:
          type: string
          enum:
            - openrouter
            - opencode-zen
            - opencode-go
            - google
            - mistral
            - mistral-vibe
        mode:
          type: string
          enum:
            - html
            - text
      required:
        - content
        - vendor

    ProcessAiTextResponse:
      type: object
      properties:
        text:
          type: string
        vendor:
          type: string
          enum:
            - openrouter
            - opencode-zen
            - opencode-go
            - google
            - mistral
            - mistral-vibe
        vendorLabel:
          type: string
        model:
          type: string
      required:
        - text
        - vendor
        - vendorLabel
        - model

    ArtPieceVersion:
      type: object
      properties:
        id:
          type: integer
        artPieceId:
          type: integer
        prompt:
          type: string
        structuredSpec:
          type: object
          additionalProperties: true
          nullable: true
        htmlCode:
          type: string
          nullable: true
        cssCode:
          type: string
          nullable: true
        generatedCode:
          type: string
        engine:
          type: string
          enum: [p5, c2, three]
        generationVendor:
          type: string
          enum:
            - openrouter
            - opencode-zen
            - opencode-go
            - google
            - mistral
            - mistral-vibe
          nullable: true
        generationModel:
          type: string
          nullable: true
        validationStatus:
          type: string
          enum: [validated]
        generationAttemptCount:
          type: integer
        notes:
          type: string
          nullable: true
        createdAt:
          type: string
      required:
        - id
        - artPieceId
        - prompt
        - generatedCode
        - engine
        - validationStatus
        - generationAttemptCount
        - createdAt

    ArtPiece:
      type: object
      properties:
        id:
          type: integer
        ownerUserId:
          type: string
        title:
          type: string
        prompt:
          type: string
        engine:
          type: string
          enum: [p5, c2, three]
        status:
          type: string
          enum: [active, archived]
        currentVersionId:
          type: integer
          nullable: true
        thumbnailUrl:
          type: string
          nullable: true
        createdAt:
          type: string
        updatedAt:
          type: string
        currentVersion:
          allOf:
            - $ref: "#/components/schemas/ArtPieceVersion"
          nullable: true
      required:
        - id
        - ownerUserId
        - title
        - prompt
        - engine
        - status
        - currentVersionId
        - thumbnailUrl
        - createdAt
        - updatedAt
        - currentVersion

    ArtPieceDetail:
      allOf:
        - $ref: "#/components/schemas/ArtPiece"
        - type: object
          properties:
            versions:
              type: array
              items:
                $ref: "#/components/schemas/ArtPieceVersion"
          required:
            - versions

    ListArtPiecesResponse:
      type: object
      properties:
        pieces:
          type: array
          items:
            $ref: "#/components/schemas/ArtPiece"
      required:
        - pieces

    GenerateArtPieceBody:
      type: object
      properties:
        prompt:
          type: string
          minLength: 1
          maxLength: 4000
        engine:
          type: string
          enum: [p5, c2, three]
        vendor:
          type: string
          enum:
            - openrouter
            - opencode-zen
            - opencode-go
            - google
            - mistral
            - mistral-vibe
      required:
        - prompt
        - engine
        - vendor

    GeneratedArtPieceDraft:
      type: object
      properties:
        draftToken:
          type: string
        title:
          type: string
        engine:
          type: string
          enum: [p5, c2, three]
        structuredSpec:
          type: object
          additionalProperties: true
          nullable: true
        htmlCode:
          type: string
          nullable: true
        cssCode:
          type: string
          nullable: true
        generatedCode:
          type: string
        notes:
          type: string
          nullable: true
        vendor:
          type: string
          enum:
            - openrouter
            - opencode-zen
            - opencode-go
            - google
            - mistral
            - mistral-vibe
        vendorLabel:
          type: string
        model:
          type: string
        validationStatus:
          type: string
          enum: [validated]
        attemptCount:
          type: integer
        maxAttempts:
          type: integer
        timedOut:
          type: boolean
        cancelled:
          type: boolean
        wasRepaired:
          type: boolean
      required:
        - draftToken
        - title
        - engine
        - generatedCode
        - notes
        - vendor
        - vendorLabel
        - model
        - validationStatus
        - attemptCount
        - maxAttempts
        - timedOut
        - cancelled
        - wasRepaired
    CreateArtPieceBody:
      type: object
      properties:
        draftToken:
          type: string
          minLength: 1
          maxLength: 191
        title:
          type: string
          minLength: 1
          maxLength: 255
          nullable: true
        prompt:
          type: string
          minLength: 1
          maxLength: 4000
          nullable: true
        engine:
          type: string
          enum: [p5, c2, three]
          nullable: true
        htmlCode:
          type: string
          nullable: true
        cssCode:
          type: string
          nullable: true
        generatedCode:
          type: string
          nullable: true
        thumbnailUrl:
          type: string
          format: uri
          maxLength: 2048
          nullable: true

    CreateArtPieceResponse:
      allOf:
        - $ref: "#/components/schemas/ArtPiece"
        - type: object
          properties:
            iframeHtml:
              type: string
          required:
            - iframeHtml

    UpdateArtPieceBody:
      type: object
      properties:
        title:
          type: string
          minLength: 1
          maxLength: 255
        prompt:
          type: string
          minLength: 1
          maxLength: 4000
        status:
          type: string
          enum: [active, archived]
        thumbnailUrl:
          type: string
          format: uri
          maxLength: 2048
          nullable: true

    CreateArtPieceVersionBody:
      type: object
      properties:
        draftToken:
          type: string
          minLength: 1
          maxLength: 191
        htmlCode:
          type: string
          nullable: true
        cssCode:
          type: string
          nullable: true
        generatedCode:
          type: string
          minLength: 1
        title:
          type: string
          minLength: 1
          maxLength: 255
        prompt:
          type: string
          minLength: 1
          maxLength: 4000
        makeCurrent:
          type: boolean

    CreateArtPieceVersionResponse:
      type: object
      properties:
        piece:
          $ref: "#/components/schemas/ArtPiece"
        version:
          $ref: "#/components/schemas/ArtPieceVersion"
        iframeHtml:
          type: string
      required:
        - piece
        - version
        - iframeHtml

    EmbeddedArtPiece:
      type: object
      properties:
        id:
          type: integer
        title:
          type: string
        engine:
          type: string
          enum: [p5, c2, three]
        version:
          $ref: "#/components/schemas/ArtPieceVersion"
      required:
        - id
        - title
        - engine
        - version

    FeedStats:
      type: object
      properties:
        totalPosts:
          type: integer
        totalComments:
          type: integer
      required:
        - totalPosts
        - totalComments

    MediaAsset:
      type: object
      properties:
        id:
          type: integer
        url:
          type: string
        filename:
          type: string
        title:
          type: string
          nullable: true
        mimeType:
          type: string
        altText:
          type: string
          nullable: true
        uploadedAt:
          type: string
      required:
        - id
        - url
        - filename
        - mimeType
        - uploadedAt

    ImportMediaBody:
      type: object
      properties:
        imageUrl:
          type: string
          format: uri
        altText:
          type: string
          nullable: true
          maxLength: 500
      required:
        - imageUrl

    UpdateMediaAltTextBody:
      type: object
      properties:
        title:
          type: string
          nullable: true
          maxLength: 255
        altText:
          type: string
          nullable: true
          maxLength: 500

    UploadedMedia:
      type: object
      properties:
        url:
          type: string
        title:
          type: string
          nullable: true
        mimeType:
          type: string
        width:
          type: integer
          nullable: true
        height:
          type: integer
          nullable: true
      required:
        - url
        - mimeType

    SiteSettings:
      type: object
      properties:
        theme:
          type: string
          enum:
            - bauhaus
            - traditional
            - minimalist
            - academic
            - airy
            - nature
            - comfort
            - audacious
            - artistic
        palette:
          type: string
          enum:
            - bauhaus
            - monochrome
            - newsprint
            - ocean
            - forest
            - sunset
            - sepia
            - high-contrast
            - pastel
        siteTitle:
          type: string
        heroHeading:
          type: string
        heroSubheading:
          type: string
        aboutHeading:
          type: string
        aboutBody:
          type: string
        copyrightLine:
          type: string
        footerCredit:
          type: string
        ctaLabel:
          type: string
        ctaHref:
          type: string
        colorBackground:
          type: string
        colorForeground:
          type: string
        colorBackgroundDark:
          type: string
        colorForegroundDark:
          type: string
        colorPrimary:
          type: string
        colorPrimaryForeground:
          type: string
        colorSecondary:
          type: string
        colorSecondaryForeground:
          type: string
        colorAccent:
          type: string
        colorAccentForeground:
          type: string
        colorMuted:
          type: string
        colorMutedForeground:
          type: string
        colorDestructive:
          type: string
        colorDestructiveForeground:
          type: string
        ownerSocialLinks:
          type: object
          additionalProperties:
            type: string
          description: |
            Map of social-platform key (instagram, twitter, youtube, tiktok,
            twitch, github, linkedin) to absolute URL, taken from the owner
            user's `social_links`. Used by the sitewide footer so it does
            not need a second round-trip or an "owner lookup" of its own.
            Empty object when no owner is set or none are populated.
        ownerWebsite:
          type: string
          nullable: true
          description: |
            The owner user's `website` URL, surfaced here so the sitewide
            footer can render a globe icon next to the social row without
            an extra round-trip.
        allowedOrigins:
          type: array
          items:
            type: string
          description: |
            Origins parsed from the `ALLOWED_ORIGINS` environment variable.
            Used by the admin UI to display the exact OAuth callback URLs
            the operator must register with each platform's developer console.
            Empty array when the env var is not set.
      required:
        - theme
        - palette
        - siteTitle
        - heroHeading
        - heroSubheading
        - aboutHeading
        - aboutBody
        - copyrightLine
        - footerCredit
        - ctaLabel
        - ctaHref
        - colorBackground
        - colorForeground
        - colorBackgroundDark
        - colorForegroundDark
        - colorPrimary
        - colorPrimaryForeground
        - colorSecondary
        - colorSecondaryForeground
        - colorAccent
        - colorAccentForeground
        - colorMuted
        - colorMutedForeground
        - colorDestructive
        - colorDestructiveForeground
        - ownerSocialLinks
        - allowedOrigins

    NavLink:
      type: object
      description: |
        A single navbar entry. The `kind` discriminator drives rendering:

          * `external` — `url` is an absolute external URL (Task #24).
          * `page`     — `pageId` and `pageSlug` are set; the public href
                         is `/p/<pageSlug>` resolved server-side via JOIN.
          * `system`   — built-in route (e.g. `/feeds`); the owner can
                         hide via `visible=false` but cannot delete.
      properties:
        id:
          type: integer
        label:
          type: string
          maxLength: 64
        url:
          type: string
          maxLength: 2048
        openInNewTab:
          type: boolean
        sortOrder:
          type: integer
        kind:
          type: string
          enum: [external, page, system]
        pageId:
          type: integer
          nullable: true
        pageSlug:
          type: string
          nullable: true
          description: Resolved at read time via JOIN on pages.id when kind='page'.
        visible:
          type: boolean
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
      required:
        - id
        - label
        - url
        - openInNewTab
        - sortOrder
        - kind
        - visible
        - createdAt
        - updatedAt

    NavLinksList:
      type: object
      properties:
        links:
          type: array
          items:
            $ref: "#/components/schemas/NavLink"
      required:
        - links

    CreateNavLinkBody:
      type: object
      properties:
        label:
          type: string
          maxLength: 64
        url:
          type: string
          maxLength: 2048
        openInNewTab:
          type: boolean
          default: true
        sortOrder:
          type: integer
          default: 0
      required:
        - label
        - url

    UpdateNavLinkBody:
      type: object
      properties:
        label:
          type: string
          maxLength: 64
        url:
          type: string
          maxLength: 2048
        openInNewTab:
          type: boolean
        sortOrder:
          type: integer
        visible:
          type: boolean

    NavItemsReorderBody:
      type: object
      description: |
        Persist a new order for every nav item. The `items` array MUST
        include every existing nav row exactly once (the server rejects
        partial reorders with `400` and lists the missing/unknown ids).
        The server applies the updates in a single transaction and
        renumbers in increments of 10 to keep gaps tidy. Clients
        typically send `sortOrder = (i+1)*10` for each item; the server
        also accepts arbitrary positive ordinals and renumbers based on
        their relative order.
      properties:
        items:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
              sortOrder:
                type: integer
            required:
              - id
              - sortOrder
      required:
        - items

    Page:
      type: object
      description: |
        A standalone CMS page rendered at `/p/:slug`. Title and slug are
        independent fields; the slug is the URL key. `contentFormat` is
        always `'html'` today (the column exists for future forks that
        want plain pages). Drafts return 404 to non-owners.
      properties:
        id:
          type: integer
        slug:
          type: string
          maxLength: 96
        title:
          type: string
          maxLength: 255
        content:
          type: string
        contentFormat:
          type: string
          enum: [html]
        status:
          type: string
          enum: [draft, published]
        showInNav:
          type: boolean
        authorUserId:
          type: string
          nullable: true
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
      required:
        - id
        - slug
        - title
        - content
        - contentFormat
        - status
        - showInNav
        - createdAt
        - updatedAt

    PagesList:
      type: object
      properties:
        pages:
          type: array
          items:
            $ref: "#/components/schemas/Page"
      required:
        - pages

    CreatePageBody:
      type: object
      properties:
        slug:
          type: string
          maxLength: 96
        title:
          type: string
          maxLength: 255
        content:
          type: string
        status:
          type: string
          enum: [draft, published]
          default: draft
        showInNav:
          type: boolean
          default: true
      required:
        - slug
        - title
        - content

    UpdatePageBody:
      type: object
      properties:
        slug:
          type: string
          maxLength: 96
        title:
          type: string
          maxLength: 255
        content:
          type: string
        status:
          type: string
          enum: [draft, published]
        showInNav:
          type: boolean

    SiteFeed:
      type: object
      description: A single subscribable feed listed in the public Feeds index.
      properties:
        slug:
          type: string
          description: Stable identifier for the feed within the catalog (e.g. "atom", "json", "mf2").
        title:
          type: string
        description:
          type: string
        url:
          type: string
          description: Absolute URL to the feed.
        mimeType:
          type: string
      required:
        - slug
        - title
        - description
        - url
        - mimeType

    SiteFeedsList:
      type: object
      properties:
        feeds:
          type: array
          items:
            $ref: "#/components/schemas/SiteFeed"
      required:
        - feeds

    PendingPost:
      type: object
      properties:
        id:
          type: integer
        authorName:
          type: string
        authorImageUrl:
          type: string
          nullable: true
        content:
          type: string
        contentFormat:
          type: string
          enum: [plain, html]
        status:
          type: string
          enum: [pending, published]
        sourceFeedId:
          type: integer
          nullable: true
        sourceGuid:
          type: string
          nullable: true
        sourceCanonicalUrl:
          type: string
          nullable: true
        sourceFeedName:
          type: string
          nullable: true
        sourceSiteUrl:
          type: string
          nullable: true
        categories:
          type: array
          items:
            $ref: "#/components/schemas/Category"
        createdAt:
          type: string
          format: date-time
      required:
        - id
        - authorName
        - content
        - contentFormat
        - status
        - categories
        - createdAt

    PendingPostsPage:
      type: object
      properties:
        posts:
          type: array
          items:
            $ref: "#/components/schemas/PendingPost"
        total:
          type: integer
        page:
          type: integer
        limit:
          type: integer
      required:
        - posts
        - total
        - page
        - limit

    ApprovePostResponse:
      type: object
      properties:
        id:
          type: integer
        status:
          type: string
          enum: [published]
      required:
        - id
        - status

    FeedSource:
      type: object
      properties:
        id:
          type: integer
        name:
          type: string
        username:
          type: string
          nullable: true
          description: Optional URL-safe handle for the feed's profile page (enables /users/@handle).
        bio:
          type: string
          nullable: true
          description: Optional short description shown on the feed's profile page.
        authorName:
          type: string
          nullable: true
          maxLength: 255
          description: Optional display name to use for all posts imported from this source. Falls back to the feed item's author, then the source name.
        feedUrl:
          type: string
        siteUrl:
          type: string
          nullable: true
        cadence:
          type: string
          enum: [daily, weekly, monthly]
        enabled:
          type: boolean
        lastFetchedAt:
          type: string
          format: date-time
          nullable: true
        nextFetchAt:
          type: string
          format: date-time
          nullable: true
          description: When this source will next be eligible for refresh (null = never fetched, treated as immediately due).
        lastStatus:
          type: string
          nullable: true
        lastError:
          type: string
          nullable: true
        itemsImported:
          type: integer
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
      required:
        - id
        - name
        - feedUrl
        - cadence
        - enabled
        - itemsImported
        - createdAt
        - updatedAt

    FeedSourcesList:
      type: object
      properties:
        sources:
          type: array
          items:
            $ref: "#/components/schemas/FeedSource"
      required:
        - sources

    PublicFeedSource:
      type: object
      description: Anonymous-safe digest of a feed source (id + display name only).
      properties:
        id:
          type: integer
        name:
          type: string
      required:
        - id
        - name

    PublicFeedSourcesList:
      type: object
      properties:
        sources:
          type: array
          items:
            $ref: "#/components/schemas/PublicFeedSource"
      required:
        - sources

    CreateFeedSourceBody:
      type: object
      properties:
        name:
          type: string
          maxLength: 255
        bio:
          type: string
          maxLength: 500
          nullable: true
          description: Optional short description shown on the feed's profile page.
        authorName:
          type: string
          maxLength: 255
          nullable: true
          description: Optional display name to use for all posts imported from this source.
        feedUrl:
          type: string
          format: uri
          maxLength: 2048
        siteUrl:
          type: string
          format: uri
          maxLength: 2048
          nullable: true
        cadence:
          type: string
          enum: [daily, weekly, monthly]
          default: daily
        enabled:
          type: boolean
          default: true
      required:
        - name
        - feedUrl

    UpdateFeedSourceBody:
      type: object
      properties:
        name:
          type: string
          maxLength: 255
        username:
          type: string
          maxLength: 30
          nullable: true
          description: Optional URL-safe handle (enables /users/@handle). Must be 2–30 lowercase letters, numbers, or underscores.
        bio:
          type: string
          maxLength: 500
          nullable: true
          description: Optional short description shown on the feed's profile page.
        authorName:
          type: string
          maxLength: 255
          nullable: true
          description: Optional display name to use for all posts imported from this source.
        feedUrl:
          type: string
          format: uri
          maxLength: 2048
        siteUrl:
          type: string
          format: uri
          maxLength: 2048
          nullable: true
        cadence:
          type: string
          enum: [daily, weekly, monthly]
        enabled:
          type: boolean

    FeedRefreshResult:
      type: object
      properties:
        sourceId:
          type: integer
        fetched:
          type: integer
        imported:
          type: integer
        skipped:
          type: integer
        status:
          type: string
          enum: [ok, error]
        error:
          type: string
          nullable: true
        alreadyInProgress:
          type: boolean
          description: |
            True when the per-source refresh endpoint was called while a
            background fetch for this source was already running. The
            endpoint returns `status: "ok"` with all counters at 0 and
            does not start a duplicate fetch.
      required:
        - sourceId
        - fetched
        - imported
        - skipped
        - status

    ApproveAllFromFeedSourceResponse:
      type: object
      properties:
        sourceId:
          type: integer
        approved:
          type: integer
          description: Number of pending posts that were flipped to published.
      required:
        - sourceId
        - approved

    FeedRefreshSummary:
      type: object
      properties:
        ranAt:
          type: string
          format: date-time
        attempted:
          type: integer
        totalFetched:
          type: integer
        totalImported:
          type: integer
        results:
          type: array
          items:
            $ref: "#/components/schemas/FeedRefreshResult"
      required:
        - ranAt
        - attempted
        - totalFetched
        - totalImported
        - results

    UpdateSiteSettingsBody:
      type: object
      properties:
        theme:
          type: string
          enum:
            - bauhaus
            - traditional
            - minimalist
            - academic
            - airy
            - nature
            - comfort
            - audacious
            - artistic
        palette:
          type: string
          enum:
            - bauhaus
            - monochrome
            - newsprint
            - ocean
            - forest
            - sunset
            - sepia
            - high-contrast
            - pastel
        siteTitle:
          type: string
          maxLength: 255
        heroHeading:
          type: string
          maxLength: 255
        heroSubheading:
          type: string
          maxLength: 1000
        aboutHeading:
          type: string
          maxLength: 255
        aboutBody:
          type: string
          maxLength: 2000
        copyrightLine:
          type: string
          maxLength: 255
        footerCredit:
          type: string
          maxLength: 255
        ctaLabel:
          type: string
          maxLength: 255
        ctaHref:
          type: string
          maxLength: 2048
        colorBackground:
          type: string
          maxLength: 64
        colorForeground:
          type: string
          maxLength: 64
        colorBackgroundDark:
          type: string
          maxLength: 64
        colorForegroundDark:
          type: string
          maxLength: 64
        colorPrimary:
          type: string
          maxLength: 64
        colorPrimaryForeground:
          type: string
          maxLength: 64
        colorSecondary:
          type: string
          maxLength: 64
        colorSecondaryForeground:
          type: string
          maxLength: 64
        colorAccent:
          type: string
          maxLength: 64
        colorAccentForeground:
          type: string
          maxLength: 64
        colorMuted:
          type: string
          maxLength: 64
        colorMutedForeground:
          type: string
          maxLength: 64
        colorDestructive:
          type: string
          maxLength: 64
        colorDestructiveForeground:
          type: string
          maxLength: 64

    PlatformConnection:
      type: object
      description: |
        An owner-managed connection to an external publishing platform.
        Encrypted credential fields are never returned; `configured: true`
        indicates a token/credential is stored.
      properties:
        id:
          type: integer
        platform:
          type: string
          enum: [wordpress_com, wordpress_self, medium, blogger, substack, bluesky, linkedin, facebook, instagram]
        configured:
          type: boolean
          description: True when an encrypted access token is stored for this connection.
        enabled:
          type: boolean
          description: When false, this connection is skipped during syndication dispatch.
        metadata:
          type: object
          additionalProperties: true
          nullable: true
          description: Platform-specific metadata (e.g. blogId, authorId, siteUrl, publicationId, authStatus, handle, personId, pageId, igUserId).
        expiresAt:
          type: string
          format: date-time
          nullable: true
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
      required:
        - id
        - platform
        - configured
        - enabled
        - createdAt
        - updatedAt

    PlatformConnectionsList:
      type: object
      properties:
        connections:
          type: array
          items:
            $ref: "#/components/schemas/PlatformConnection"
      required:
        - connections

    CreatePlatformConnectionBody:
      type: object
      description: |
        Body for credential-based platform connections (`wordpress_self`, `medium`, `substack`).
        OAuth-based platforms use the `/api/platform-oauth/{platform}/start`
        redirect flow instead.
      properties:
        platform:
          type: string
          enum: [wordpress_self, medium, substack]
        credentials:
          type: object
          description: Credentials for the target platform.
          properties:
            siteUrl:
              type: string
              format: uri
            username:
              type: string
            appPassword:
              type: string
            token:
              type: string
              description: Self-integration token (Medium only).
            sessionCookie:
              type: string
              description: Substack `connect.sid` session cookie value.
            publicationId:
              type: string
              description: Numeric Substack publication ID to target when publishing.
            publicationHost:
              type: string
              description: Substack publication hostname used for publication-scoped draft and publish writes.
      required:
        - platform

    PatchPlatformConnectionBody:
      type: object
      properties:
        enabled:
          type: boolean
          description: Set to false to pause syndication to this platform without disconnecting.

    PostSyndication:
      type: object
      description: Result of one syndication attempt for a single post + platform pair.
      properties:
        id:
          type: integer
        postId:
          type: integer
        platformConnectionId:
          type: integer
        status:
          type: string
          enum: [pending, success, failed]
        externalId:
          type: string
          nullable: true
          description: The post's ID on the external platform (when available).
        externalUrl:
          type: string
          nullable: true
          description: The post's canonical URL on the external platform.
        errorMessage:
          type: string
          nullable: true
        syncedAt:
          type: string
          format: date-time
          nullable: true
        createdAt:
          type: string
          format: date-time
      required:
        - id
        - postId
        - platformConnectionId
        - status
        - createdAt

    PostSyndicationsList:
      type: object
      properties:
        syndications:
          type: array
          items:
            $ref: "#/components/schemas/PostSyndication"
      required:
        - syndications

    PostSyndicationBadge:
      type: object
      description: Lightweight summary of a successful cross-post, embedded in post list responses.
      properties:
        platform:
          type: string
          enum: [wordpress_com, wordpress_self, medium, blogger, substack, bluesky, linkedin, facebook, instagram]
        externalUrl:
          type: string
          nullable: true
          description: The post's URL on the external platform, if available.
      required:
        - platform

    PlatformOAuthApp:
      type: object
      description: |
        Site-wide OAuth application credentials for a POSSE platform.
        CLIENT_ID and CLIENT_SECRET are stored encrypted and never returned;
        `configured: true` means both are saved and the OAuth flow can proceed.
      properties:
        platform:
          type: string
          enum: [wordpress_com, blogger, linkedin, facebook]
        configured:
          type: boolean
        blogUrl:
          type: string
          nullable: true
          description: Blog URL used to scope the OAuth token (e.g. https://yourblog.wordpress.com).
      required:
        - platform
        - configured

    PlatformOAuthAppsList:
      type: object
      properties:
        apps:
          type: array
          items:
            $ref: "#/components/schemas/PlatformOAuthApp"
      required:
        - apps

    UpsertPlatformOAuthAppBody:
      type: object
      description: CLIENT_ID and CLIENT_SECRET for an OAuth platform's developer app registration.
      properties:
        clientId:
          type: string
          minLength: 1
          maxLength: 512
        clientSecret:
          type: string
          minLength: 1
          maxLength: 512
        blogUrl:
          type: string
          maxLength: 500
          description: Your blog URL (e.g. https://yourblog.wordpress.com). Used to scope the OAuth token to the correct blog.
      required:
        - clientId
        - clientSecret
